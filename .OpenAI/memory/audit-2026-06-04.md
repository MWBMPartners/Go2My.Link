---
name: audit-2026-06-04
description: "2026-06-04 full deployment-readiness audit — report, 28 issues filed, launch verdict, fix branch"
metadata: 
  node_type: memory
  type: project
  originSessionId: be661796-f488-4097-968b-e026933459e3
---

On 2026-06-04 a full deployment-readiness audit ran (deep multi-agent workflow: code quality, security, accessibility, GitHub-issue conformance for all 91 issues, and project-brief conformance, across components A/B/C).

**Report:** `docs/AUDIT_2026-06-04.md` (in repo).

**Launch verdict:** Components A (go2my.link) + B (g2my.link) are launchable after a small fix set. Component C (lnks.page / LinksPage) is **not built** (Phase 8 not started) and must not be advertised beyond its coming-soon landing page.

**28 issues filed (#93–#120)** from audit findings. Milestones: critical/high/medium → new **"v1.0.0 — Launch Hardening"** (milestone #13); low → v1.1.0 (Phase 7). All added to org Project #4.

**P0/P1 fixes applied** on branch `audit/launch-hardening-2026-06-04` (committed, not pushed): link-edit `notes`→`urlNotes` column bug (#94); contact-form CRLF subject strip + server-side CAPTCHA verify (#96/#97); Component B favicon `logo.png` fallback (#102); Component B CSP allows jsDelivr/cdnjs + inline (#103); `release.yml` lint tool `php-parallel-lint`→`parallel-lint` (#111); `.gitignore` guards for `**/dbConfig.php` + `**/public_html_legacy/` (#93).

**Still a MANUAL user action (not done by agent):** rotate the legacy production DB credential in `web/G2My.Link/public_html_legacy/dbConfig.php` (#93). It was verified **never committed to git history**, is now gitignored, but sat in plaintext on disk; treat as compromised.

**Closure-protocol drift found:** several *closed* issues are only partial — #23 (info-page public/auth view), #30 (dashboard custom suffix/alias/tags not exposed), #91 (custom-domain resolution only queries `tblOrgShortDomains`), plus legal docs still draft (`{{LEGAL_REVIEW_NEEDED}}`). #92 (UTM) was wrongly recorded complete — corrected (see [[MEMORY]]).
