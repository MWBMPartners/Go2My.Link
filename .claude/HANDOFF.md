# 🤝 The handoff has moved

The project's one handoff is **[`.github/HANDOFF.md`](../.github/HANDOFF.md)**.

It moved there on 2026-09-22 (owner decision 17, GitHub issue #243), because the dev-team plugin
writes and commits its own summary card at `HANDOFF.md` in the repository root and would overwrite
whatever was there. `.github/` is out of its way, and the root file is now ignored by git.

This file is a signpost, not a second handoff: never write session state here.
